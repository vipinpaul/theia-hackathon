import { injectable } from '@theia/core/shared/inversify';

@injectable()
// Add contribution interface to be implemented, e.g. "AudioExtensionContribution implements CommandContribution"
export class AudioExtensionContribution{

}